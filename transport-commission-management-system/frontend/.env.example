# Frontend API Base URL Configuration
# Leave blank or omit in development to rely on Vite dev proxy (/api -> http://localhost:3000)
# Set in production to target deployed backend server URL (e.g., https://api.your-company-domain.com)
VITE_API_URL=http://localhost:3000
